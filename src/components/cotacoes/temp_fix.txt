Restaurar o comportamento original da planilha consolidada:

Antes: Botão "Gerar Planilha Consolidada" → Abre DialogPlanilhaConsolidada → Usuário escolhe média/mediana/menor preço e global/por item → Gera planilha

Agora (errado): Botão → Gera direto sem perguntar

Solução: Substituir a função gerarPlanilhaConsolidada para apenas abrir o dialog.

Linha 202-379 de DialogRespostasCotacao.tsx deve se tornar:
const gerarPlanilhaConsolidada = async () => {
  setPlanilhaConsolidadaOpen(true);
};
